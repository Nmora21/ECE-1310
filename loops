#include <iostream>
using namespace std;

int main()
{
    int i = 0;
    int sum = 0;
    while (i <= 10)
    {
        sum = sum + i;
        cout << "The sum is now " << sum << endl;
        i++;
    }
}
